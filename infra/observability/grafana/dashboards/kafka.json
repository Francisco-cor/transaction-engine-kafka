{
  "title": "Kafka - Lag and DLT",
  "uid": "kafka-lag",
  "tags": ["kafka", "plg"],
  "panels": [
    { "type": "timeseries", "title": "Consumer lag sum", "targets": [{ "expr": "sum(kafka_consumer_lag) by (group)", "refId": "A" }] },
    { "type": "stat", "title": "DLT rate", "targets": [{ "expr": "increase(kafka_DLT_records_total[5m])", "refId": "A" }] },
    { "type": "timeseries", "title": "Outbox backlog max", "targets": [{ "expr": "max(outbox_pending_events) by (job)", "refId": "A" }] },
    { "type": "timeseries", "title": "Oldest message age", "targets": [{ "expr": "kafka_consumer_oldest_message_age_seconds", "refId": "A" }] }
  ],
  "schemaVersion": 39
}
